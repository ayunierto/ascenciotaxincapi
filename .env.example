# =============================================================================
# ASCENCIO TAX INC. API - ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# IMPORTANT: This is a template file. DO NOT add real credentials here.
# Copy this file to .env and replace all placeholder values with real ones.
# Command: cp .env.example .env (Linux/Mac) or copy .env.example .env (Windows)
# =============================================================================

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------
PORT=3000
STAGE=development # Options: development, production, staging, local

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Full database connection URL (use this OR individual variables below)
# Format: postgresql://username:password@host:port/database
DB_URL=postgresql://postgres:your_secure_password@localhost:5432/your_database_name

# Individual database settings (used by Docker Compose)
DB_PASSWORD=your_secure_password
DB_NAME=your_database_name

# -----------------------------------------------------------------------------
# JWT (JSON Web Token) CONFIGURATION
# -----------------------------------------------------------------------------
# Generate a strong secret key (recommended: 32+ characters)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# More info: https://jwt.io/
JWT_SECRET=REPLACE_WITH_RANDOM_STRING_MIN_32_CHARACTERS

# Token expiration time
# Expressed in seconds or a string describing a time span zeit/ms
# Examples: 60, "2 days", "10h", "7d", "60m"
# More info: https://github.com/vercel/ms
JWT_EXPIRY=60m

# -----------------------------------------------------------------------------
# EMAIL VERIFICATION SETTINGS
# -----------------------------------------------------------------------------
# Expiration time for email verification codes (in minutes)
EMAIL_VERIFICATION_EXPIRY=15

# -----------------------------------------------------------------------------
# EMAIL SERVICE CONFIGURATION (SMTP)
# -----------------------------------------------------------------------------
# Use an app-specific password, NOT your regular email password
# For Gmail: https://support.google.com/accounts/answer/185833
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_specific_password
SENDER_NAME="Ascencio Tax Inc."

# -----------------------------------------------------------------------------
# BUSINESS SETTINGS
# -----------------------------------------------------------------------------
# Default slot duration for appointments (in minutes)
SLOT_STEP_MINUTES_DEFAULT=15

# Business timezone (IANA timezone format)
# Examples: America/New_York, America/Toronto, America/Los_Angeles
BUSINESS_TZ=America/Toronto

# -----------------------------------------------------------------------------
# CLOUDINARY CONFIGURATION (Image/File Storage)
# -----------------------------------------------------------------------------
# Get your credentials at: https://console.cloudinary.com/
CLOUDINARY_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# -----------------------------------------------------------------------------
# GOOGLE SERVICES CONFIGURATION
# -----------------------------------------------------------------------------
# Create a service account at: https://console.developers.google.com/
# Download credentials.json and extract the values
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Copy the entire private key from credentials.json (including header and footer)
# Keep the \n characters for line breaks
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_CONTENT_HERE\n-----END PRIVATE KEY-----\n"

# Your Google Calendar ID (found in calendar settings)
GOOGLE_CALENDAR_ID=your_calendar_id@group.calendar.google.com

# -----------------------------------------------------------------------------
# ZOOM API CONFIGURATION
# -----------------------------------------------------------------------------
# Create a Server-to-Server OAuth app at: https://marketplace.zoom.us/user/build
ZOOM_ACCOUNT_ID=your_zoom_account_id
ZOOM_CLIENT_ID=your_zoom_client_id
ZOOM_CLIENT_SECRET=your_zoom_client_secret

# -----------------------------------------------------------------------------
# OPENAI API CONFIGURATION
# -----------------------------------------------------------------------------
# Get your API key at: https://platform.openai.com/api-keys
# NEVER commit this key to version control
OPENAI_API_KEY=sk-proj-YOUR_OPENAI_API_KEY_HERE

# =============================================================================
# SECURITY REMINDERS:
# - NEVER commit the .env file to version control
# - Add .env to .gitignore (should already be there)
# - Use strong, unique passwords for all services
# - Rotate secrets regularly, especially in production
# - Use environment-specific .env files (.env.dev, .env.prod)
# - Consider using a secrets manager for production (AWS Secrets Manager, etc.)
# =============================================================================

